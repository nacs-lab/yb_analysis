"""Lab-side control plumbing shared across UIs (Tkinter + web dashboard).

Currently houses :mod:`yb_analysis.control.memmap_signal`, the single source
of truth for the MATLAB MemoryMap byte layout used to signal Pause / Abort /
Start to the running sequence.
"""
